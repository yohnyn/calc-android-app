# CURRENT_TASK

## 最近任务

完成首页结果、方案库联动、开仓方案对比和补仓助手的交互收口，并同步清理全部项目文档，使其与当前源码一致。

## 已完成

- 首页结果卡将 `保存方案` 与 `查看详情` 拆为独立点击区，保留明确点击反馈。
- 保存当前方案时按完整参数去重；重复方案不写入并提示用户。
- 结果详情展示计算后的 `当前仓位保证金` 与 `仓位币数量（按开仓价）`，不展示币数量公式。
- 方案对比不再依赖首页参数完整；任意完整方案可被选择并设为基准，对比结果和复制摘要按基准计算。
- 方案库保存和打开保留完整输入；删除当前打开方案或全部方案时同步清理仍与其关联的首页、对比和补仓状态。
- 补仓助手重排为 `当前持仓 → 补仓设置 → 补仓后预览 → 可选收益模拟`。
- 补仓当前持仓支持 `手动创建 / 从方案库选择`，手动数据仅用于本次模拟，不写入方案库。
- 结果详情提供 `模拟补仓`，方案库提供 `带入补仓助手`；两者会带入数据、返回首页并定位补仓助手。
- 普通展开补仓助手不会隐式带入或覆盖首页结果。
- 补仓支持按金额、币数量和当前保证金 `25% / 50% / 100%` 快捷填写；比例金额保留两位小数。
- 补仓目标平仓价改为可选；未填写时仍可计算均价、数量、保证金和仓位变化。
- 补仓数量最多显示六位小数；当前持仓编辑中保证金最多两位、币数量最多六位。
- 移除方案带入后的重复“已填入”提示；补仓结果详细数据将当前杠杆与新仓位价值同排。
- 项目文档已删除旧版本流水账、重复规则和与当前实现冲突的描述。

## 静态验证

- 已执行非编译源码与文档检查。
- 已检查文档中的旧交互关键词和冲突描述。
- 已执行 `git diff --check`。

## 未执行

按 `AGENTS.md` 要求，代理未运行 Gradle、编译、安装或测试命令。

## 待用户本地验证

- Android Studio Sync、编译和单元测试。
- 首页结果卡保存/查看详情操作区与重复方案提示。
- 不完整首页参数下的方案导入、选择基准和对比结果。
- 打开方案后删除单个方案、全部删除及手动修改后解除来源关联。
- 补仓助手手动创建、方案带入、结果详情带入、折叠/展开、页面定位和完整结果弹窗。
- 小额 BTC 仓位与补仓数量的小数显示。

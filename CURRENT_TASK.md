# CURRENT_TASK

## 最近任务

完成用户体验审视第 1 至第 4 阶段优化、全 App 数字展示精度修复，并对当天修改进行集中静态审查。

## 已完成

- 明确保证金与币数量输入语义，增加输入错误态、整数杠杆限制和适配性更好的可选计算布局。
- 首页增加渐进结果状态；完整结果卡分别展示净盈亏与保证金收益率。
- 首页默认折叠方案对比，底部固定保留 `记录 / 方案 / 设置`，支持作者入口继续保留。
- 首页对比卡只负责选择与编辑；结果页选择基准；不完整方案不能加入或保存修改。
- 方案库卡片仅直接展示 `打开 / 加入对比`，其余操作收进更多菜单。
- 查看完整收益、币本位、补仓或对比结果后自动加入记录，使用规范化参数指纹跨重启去重。
- 统一金额、价格、币数量、百分比、费率、比率和杠杆展示规则；兜底最多六位，杠杆仅允许整数。
- 设置首页展示当前选项值；统一使用“账户总资金”；增加强平与可开的动态短说明。
- 主结果详情使用固定顶部关闭入口、独立滚动内容和固定底部操作栏；币本位详情增加固定关闭入口。
- 首页结果操作文案由 `保存方案` 调整为 `存为方案`，与自动加入记录区分。

## 本轮静态审查修复

- 修复一体式保证金/币数量输入超过精度时，界面文本与实际计算状态不一致的问题。
- 补齐补仓助手对整数杠杆和 1x 至 125x 范围的校验。
- 自动记录指纹改为规范化参数，避免相同数值因尾零不同而重复保存。
- 对比记录指纹不再受临时方案 ID 和选择顺序影响。
- 币本位历史快照补齐保证金模式与杠杆，避免转换方案时使用错误默认值。
- 逐仓开启可开时，结果详情可展示实际参与计算的账户总资金。
- 对比、补仓和自定义币种价格输入补齐正数即时错误反馈。

## 静态验证

- 已检查旧交互文案、数字格式化调用、结果打开与自动记录入口、杠杆校验和历史构造调用。
- 已执行 `git diff --check`。

## 未执行

按 `AGENTS.md` 要求，代理未运行 Gradle、编译、安装或测试命令。

## 待用户本地验证

- Android Studio Sync、编译和单元测试。
- 各数字输入的小数位限制、错误态和极小非零数字展示。
- 主结果详情固定头部、固定底部操作栏与键盘/小屏适配。
- 自动记录新增提示、重复打开去重、删除后重新加入和重启后去重。
- 对比方案完整性校验、结果页切换基准和方案库更多菜单。
- 全仓/逐仓强平与可开组合下的账户总资金说明和展示。
